version: 0.2
phases:
  install:
    commands:
      - echo "Installing dependencies..."
      - npm install  # or pip install -r requirements.txt
  build:
    commands:
      - echo "Building application..."
      - npm run build  # or mvn package for Java
  post_build:
    commands:
      - echo "Build complete. Preparing artifact..."
artifacts:
  files:
